<%= form_for @post, html: { multipart: true } do %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_for @post, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>
  <div class="field">
    <div class="field">
      <h3>Image:</h3>
      <%= f.file_field :image %>
    </div>
    <div class="field">
      <h3>Title:</h3>
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <h3>Description:</h3>
      <%= f.text_area :description, size: "60*24" %>
    </div>
    <div class="field">
      <%= f.submit "Create" %>
    </div>
  <% end %>

<% end %>
